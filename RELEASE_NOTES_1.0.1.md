# BoxMate 1.0.1

## Fixed

- GitHub sign-in no longer sends its OAuth token to public `raw.githubusercontent.com` manifest downloads.
- The official catalogue and dependency manifests now load correctly while signed in.
- Authenticated requests remain enabled for GitHub's release API, retaining the higher 5,000-request hourly limit.

The Windows and Linux downloads contain the complete self-contained runtime output.
