# BoxMate v1.4.6

- Fix dependency version checks when a manifest uses a GitHub repository link rather than a direct raw manifest URL.
- Replace the unhelpful `Sequence contains no matching element` failure with a clear dependency-resolution error.
- Keep Experimental project cards and warnings introduced in 1.4.5.

Includes complete self-contained Windows and Linux runtimes. Linux/Steam Deck interaction has not been tested for this release.
